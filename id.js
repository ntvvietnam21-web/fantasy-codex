function createKingdomID(){
return "kingdom_" + Date.now() + "_" + Math.floor(Math.random()*1000);
}

function createFactionID(){
return "faction_" + Date.now() + "_" + Math.floor(Math.random()*1000);
}